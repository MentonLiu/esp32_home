# 变更说明

## 2026-03-23

### 根目录文档重构

1. 通读近期 git 提交历史，重新梳理仓库从“多方案并行”到“服务端 HTTP 主链路优先”的演进脉络。
2. 重写根目录 `readme.md`，补充仓库角色划分、当前实现状态、推荐阅读顺序和近期演进摘要。
3. 重写 `doc/README.md`，明确哪些文档是现状入口、哪些文档是历史需求原文。
4. 新增 `doc/快速上手.md`，方便新接手者快速跑通服务端和理解联调顺序。
5. 新增 `doc/shared/系统总览与联调指南.md`，把浏览器、客户端、服务端和 ESP8266 红外桥的关系串起来。
6. 明确标注文档现状：`esp8266_ir_control/` 目录已建立，但当前仍是占位工程，不应误解为已完成模块。
7. 保留并强化“MQTT 仍是预留能力、不是当前主链路”的口径，避免新读者从旧文档误判项目重心。

### 根目录入口优化（补充）

1. 重构根目录 `readme.md` 的首页结构，增加“基于 Git 的近期演进”摘要，突出当前可运行主链路。
2. 新增 `doc/版本演进与里程碑.md`，按提交时间线分阶段说明项目演进背景与当前状态。
3. 更新 `doc/README.md` 导航，把“快速上手 + 演进里程碑 + 联调总览”组合为统一阅读路径。

## 2026-03-21

### 文档与结构调整

1. 统一调整根目录、服务端、客户端和共享对接文档的描述口径。
2. 固定家庭 Wi-Fi 为 `iPadmini / lbl450981`。
3. 明确当前主链路是“服务端 HTTP + 客户端面板”，MQTT 暂缓。
4. 明确红外控制为“服务端串口发送字符串命令到 ESP8266”。
5. 明确 8pin TFT 未来使用 LVGL，但当前显示逻辑先屏蔽。
6. 新增根目录结构说明页 `project-map.html`，作为整个仓库的结构文件说明。

### 后续实现目标

1. 服务端删除与旧结构页关联的网页路由和资源依赖。
2. 服务端移除客户端时间显示相关字段。
3. 服务端弱化 MQTT 实际执行代码，仅保留占位接口。
4. 服务端和客户端同步改为红外字符串命令。
5. 为风扇增加电源状态控制，客户端新增对应按钮逻辑。
6. 客户端屏蔽 TFT/LVGL 未定界面，保留 LCD1602 和 RGB。
