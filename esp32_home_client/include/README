# Client `include/`

这个目录保存客户端的头文件声明，当前按职责拆成几组：

- `ClientConfig.h`
  客户端固定配置：Wi-Fi、引脚、按钮命令、显示开关。
- `ClientContracts.h`
  客户端内部共享的数据结构和枚举。
- `ClientWiFiManager.h`
  管理 Wi-Fi 连接与重连策略。
- `ServerApiClient.h`
  负责服务端发现、状态拉取和控制命令发送。
- `InputManager.h`
  按键、风扇按钮、旋钮事件抽象。
- `OutputManager.h`
  RGB、LCD1602 和被屏蔽的 TFT 输出。
- `HomeClientApp.h`
  顶层调度器声明。

如果你要改硬件接线或按钮语义，优先先看 `ClientConfig.h`。
